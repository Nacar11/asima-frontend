// Public API for the `admin-compensation` slice. Deliberately empty: no
// other slice consumes it today. When one does, export EXACTLY the symbols
// it needs here — do not `export *` (a curated surface is the point of the
// barrel; see frontend-architecture.md §3 rule 4).
export {};
